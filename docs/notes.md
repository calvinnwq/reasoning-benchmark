# Project Notes

## Naming decisions

- `data/questions.json` is the canonical dataset path
- `data/questions.csv` is the spreadsheet-friendly export
- `docs/benchmark.md` is the main human-readable overview
- `runs/` is for raw model outputs, scored outputs, and analysis artifacts

## Immediate priorities

1. prune weak items
2. define a scorer contract
3. add provider/model runner adapters
4. generate first baseline runs
